```
sudo systemctl stop 0gchaind geth
```
```
cd $HOME
wget https://github.com/0gfoundation/0gchain-NG/releases/download/v3.0.2/galileo-v3.0.2.tar.gz && tar -xvzf galileo-v3.0.2.tar.gz
rm -rf galileo-v3.0.2.tar.gz
mv $HOME/galileo-v3.0.2/validator/bin/* $HOME/go/bin/
rm -rf $HOME/galileo-v3.0.2
chmod +x $HOME/go/bin/*
```
```
cd $HOME
sudo systemctl daemon-reload
sudo systemctl restart geth
sudo systemctl restart 0gchaind
```
```
sudo journalctl -u 0gchaind -u geth -f
```
